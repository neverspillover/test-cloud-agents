# Test Cloud Agents

该仓库用于验证 Cursor Cloud Agent 的协作流程。

Cursor Cloud Agent 对仓库类型没有限制，fork 仓库同样适用。若要在 Cursor 客户端中使用 Cloud Agent，请确保本地 Git remote 配置正确（例如存在指向 GitHub 的 `origin` 远程）。如果 SSH 配置将远程连接名改成自定义域名，Cloud Agent 可能无法识别并建立连接。
